services:
  - type: web
    name: rag-gov-assistant
    runtime: python
    buildCommand: ./build.sh
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: WEB_CONCURRENCY
        value: 1